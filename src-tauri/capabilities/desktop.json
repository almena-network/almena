{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "What the main window may call on a computer, and on a computer alone. Apart from `default` because a permission naming a plugin that is not in the mobile binary is a build that does not resolve, so the platforms are named here rather than the file being left to apply everywhere.",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": ["dialog:default", "updater:default"]
}
